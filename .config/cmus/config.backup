# Autoconfig
scroll_offset (2) 8

# :Command
bind -f common h win-next
bind -f common l win-next
bind common < seek -1m
bind common > seek +1m
bind -f common , seek -5
bind -f common . seek +5
bind -f common u shell ~/.config/cmus/update-lib

colorscheme flip
